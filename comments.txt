Some comments:

As MJD-OBS has several digits, for readability, pls display it as a vertical label. Allow also to have normal dates. In view of long-term operations, I would allow also to set start/end MJD-OBS/dates to display, not to have the plot too crowded.

In case of flat-VIS, you can have several group values. What about having a slider to be able to access them all on a fixed vertical size?

Now the QC plots are values vs MJD-OBS. What about allowing also them to plot vs relevant other variables (relevant FITS keyword)? For example, some possibly correlated temperature, other QC parameter, relevant FITS keyword? Would allow to understand possible future problems versus most relevant possibly correlated quantities.

I tried to zoom in some region, but got a problem, the plot was not redisplayed

You may display outliers to the ks-clip mean with a special symbol/color

For efficiency QC, I would allow to also display the plot over the full range, to spot problems on a given range. Nice would be, if not too complex, to access it by clicking on a data point (now for example you show points on the median efficiency).

On the proper recipe I would add a plot with the spectral resolving power (ideally at several wavelengths)

As you did, I suggest defining appropriate thresholds to monitor the most important QC parameters that are fixed for a given instrument setting. While checking outliers to ks-clip mean maybe useful, the instrument may drift, and thus better to have fixed thresholds

I see you have QC also for not (and stare?) why on science (property data) ?

I would allow to display (maybe on demand, to keep page update rate short) also images of selected QC products.

To build/load the QC page faster, you may decide to display less information and just list in a menu/option list what else the user may want to look at